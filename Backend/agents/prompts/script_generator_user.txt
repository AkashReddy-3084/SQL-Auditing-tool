Generate the audit script for this checklist item.

**Checklist ID**: {checklist_id}
**Category**: {category}
**Check Name**: {check_name}
**Description**: {description}
**Expected Outcome**: {expected_outcome}
**Declared Scope**: {scope}

Follow the system prompt exactly: write the 5-line ANALYSIS first, then the FEASIBLE determination with the documentation gate applied before the artifact gate, then — only when FEASIBLE: YES — the complete read-only script between ---SCRIPT_START--- and ---SCRIPT_END--- ending in the four-column output SELECT.

Never skip the ANALYSIS. Use the documentation gate correctly: mark `FEASIBLE: NO, IS_DOCUMENTATION_CHECK: YES` ONLY when the wording explicitly asks about a **human decision, approval, review, sign-off, or formal documentation** that cannot exist in the database (e.g., "was X approved?", "is X documented?", "was Y tested and signed off?"). For all other checks—even if phrased as "is X present" or "does X follow Y architecture"—proxy evidence from observable database patterns counts as sufficient. Schema naming, table structure, foreign keys, extended properties, procedure dependencies, and column patterns are all valid evidence for architectural and design checks. Generate a script unless the check is literally asking whether a record or approval exists outside the database.

Keep the ANALYSIS short so the whole script fits in one answer, and simplify the evaluation rather than leaving the script unfinished.
