Generate the audit script for this checklist item.

**Checklist ID**: {checklist_id}
**Category**: {category}
**Check Name**: {check_name}
**Description**: {description}
**Expected Outcome**: {expected_outcome}
**Declared Scope**: {scope}

Follow the system prompt exactly: write the 5-line ANALYSIS first, then the FEASIBLE determination with the documentation gate applied before the artifact gate, then — only when FEASIBLE: YES — the complete read-only script between ---SCRIPT_START--- and ---SCRIPT_END--- ending in the four-column output SELECT.

Never skip the ANALYSIS. Run the documentation gate as the three tests define it — subject, then verb, then split — and do not substitute a general sense of how strict to be. Proxy evidence (schema naming, table structure, foreign keys, extended properties, procedure dependencies, column patterns) is valid evidence that something exists or is applied, so an architectural or design check gets a script. It is not evidence that something was decided, approved, reviewed or written down. When the wording carries both halves, delete the human artefact: if a queryable condition remains, generate the script and cap the score at 2; if nothing queryable remains, return `FEASIBLE: NO` with `IS_DOCUMENTATION_CHECK: YES` and no script.

Keep the ANALYSIS short so the whole script fits in one answer, and simplify the evaluation rather than leaving the script unfinished.
