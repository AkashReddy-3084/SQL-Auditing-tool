<Window x:Class="SQLAuditor.Wpf.EvidenceSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Evidence Indexed"
        Height="560" Width="760"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#FFF4F7FB">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock x:Name="HeadlineText" Text="Evidence indexed" FontSize="20" FontWeight="Bold" />
            <TextBlock x:Name="SubheadText" Text="" Foreground="#FF6B7280" TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="White" BorderBrush="#FFD9E1EC" BorderThickness="1" CornerRadius="10" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="DetailText"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         FontSize="12" />
            </ScrollViewer>
        </Border>

        <Button Grid.Row="2" x:Name="OkButton" Content="OK" Width="120" Height="38" Margin="0,16,0,0"
                HorizontalAlignment="Right" IsDefault="True" IsCancel="True" Click="OkButton_Click" />
    </Grid>
</Window>
